@{
      var db = Database.Open("edata");
      var proId = UrlData[0];

      var cat = db.QuerySingle("Delete FROM products WHERE prodId = @0", proId);
       
      Response.Redirect("~/adminPanel/pages/manageProducts");    
}
